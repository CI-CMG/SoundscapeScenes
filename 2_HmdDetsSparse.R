# label ambient 1-min HMD with sparse signals present 

# read in all HMD data... one giant matrix

# index for ambient rows- only select these rows

# run Non-negative matrix decomposition

# label each as sparse or ambient 